package com.work.springbootinit.utils;

import cn.hutool.core.date.DateUtil;

import java.time.DayOfWeek;
import java.time.LocalDate;
import java.time.format.DateTimeFormatter;

import java.time.temporal.TemporalAdjusters;
import java.util.concurrent.TimeUnit;


public class TestUtil {
    public static void main(String[] args) {
//        DateTimeFormatter formatter = DateTimeFormatter.ofPattern(DateUtil.PATTERN_YYYYMMDD);
//        String lastWeekMondayDateStr = LocalDate.now().minusDays(7)
//                .with(TemporalAdjusters.previousOrSame(DayOfWeek.MONDAY)).format(formatter);
    }
}
